#include<iostream>
using namespace std; 
int main ()
{
    int n, binary;
    cout << "Enter the decimal number : ";
    cin >> n;
    cout << "The binary equivalent of " << n ;
    while (n > 0)
    {
        binary = n % 2;
        n = n/2;
        cout << binary;
       
    }
    return 0;
}
